import React from 'react';
import Header from '../Header/Header';

const Login = () => {
    return (
        <div>
            <Header></Header>
            <h1>Login is not work now.<strong style={{color:'red'}}>জিংগালালা....হু..হু।</strong></h1>
        </div>
    );
};

export default Login;